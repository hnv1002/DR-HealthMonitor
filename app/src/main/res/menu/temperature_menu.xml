<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/temperature_graph_options"
        android:title="Scale"
        app:showAsAction="always">
        <menu>
            <item
                android:id="@+id/temperature_c_scale"
                android:onClick="cScale"
                android:title="C" />
            <item
                android:id="@+id/temperature_f_scale"
                android:onClick="fScale"
                android:title="F" />
        </menu>
    </item>

    <item
        android:id="@+id/temperature_home_button"
        android:icon="@drawable/ic_home"
        android:iconTint="@android:color/black"
        android:onClick="goToHomeView"
        android:title="Home"
        app:showAsAction="always" />

</menu>